version: '3.5'

services:
  web_app:
    build: .
    image: app
    container_name: app
    hostname: app
    environment:
      CHECK_METHOD: ${METHOD}
    tty: true
    ports:
      - "5000:5000"
    volumes:
      - .:/code
    networks:
      - esnet
networks:
  esnet: